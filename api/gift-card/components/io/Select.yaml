properties:
  context:
    allOf:
      #adding Enum for FSI
      - properties:
          domain:
            $ref: '../enum/index.yaml#/Domain'
      - properties:
          location:
            properties:
              country:
                allOf:
                  - $ref: '../enum/index.yaml#/Country'
                  - required:
                    - code
            required:
              - country
  message:
    properties:
      order:
        allOf:
          - properties:
              items:
                # minimum 1 item required
                minItems: 1
                items:
                  required:
                    - id
          - required:
              - items